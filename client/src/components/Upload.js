import React from 'react';

class Upload extends React.Component {

    handleForm(e) {
        e.preventDefault();
    }

    render() {
        return (
            <form>

            </form>
        );
    }
}

export default Upload;